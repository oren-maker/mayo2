{
  "version": 2,
  "builds": [
    { "src": "server.mjs", "use": "@vercel/node", "config": { "maxDuration": 300 } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server.mjs" }
  ],
  "crons": [
    { "path": "/api/cron/daily-refresh", "schedule": "0 5 * * *" }
  ]
}
